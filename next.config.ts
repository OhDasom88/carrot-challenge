const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "avatars.githubusercontent.com",
      },
    ],
  },
  experimental: {
    optimizeFonts: false,
  },
};


export default nextConfig;
